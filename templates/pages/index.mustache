{{<layout}}
{{$main}}
    <section id="gallery">
        <div class="gallery-container">
          {{#galleryCards}}
          <article class="gallery-card">
            <div class="card-header">
              <div class="header-left">
                <img src="{{avatar}}" alt="Fotógrafo" class="profile-avatar">
                <div class="header-info">
                  <span class="photographer-name">{{name}}</span>
                  <span class="photographer-location">{{location}}</span>
                </div>
              </div>
              <button class="card-menu">...</button>
            </div>
            <div class="card-image">
              <img src="{{image}}" alt="Fotografia">
            </div>
            <div class="card-footer">
              <div class="card-actions">
                <button class="action-btn">❤️ {{likes}}</button>
                <button class="action-btn">💬 {{comments}}</button>
                <button class="action-btn">📤</button>
              </div>
              <div class="card-details">
                <strong>{{photos}} fotos • {{category}}</strong>
              </div>
            </div>
          </article>
          {{/galleryCards}}
        </div>
      </section>
{{/main}}
{{/layout}}