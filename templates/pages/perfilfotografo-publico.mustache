{{< layout}}
{{$main}}

<main class="profile-page">
  <div class="header-voltar" style="display: flex; align-items: center; gap: 12px; margin-bottom: 16px;">
    <img id="header-avatar" src="assets/images/default.png" style="width:40px;height:40px;border-radius:50%;object-fit:cover;">
    <a id="voltar-perfil" href="#" class="back-link">Voltar ao Meu Perfil</a>
  </div>

  <section class="profile-card">
    <img id="avatar" src="https://i.pravatar.cc/80?img=32" class="avatar">

    <div class="profile-info">
      <h2 id="nome">A carregar...</h2>
      <p id="descricao"></p>

      <div class="meta">
        <span id="avaliacoes"></span>
        <span id="localizacao"></span>
      </div>
    </div>
  </section>

  <section class="tabs">
    <button class="tab-btn active" data-tab="portfolio">Portfólio</button>
    <button class="tab-btn" data-tab="servicos">Serviços</button>
    <button class="tab-btn" data-tab="avaliacoes">Avaliações</button>
  </section>

  <section class="tab-content">
    <div id="portfolio" class="tab-panel active">
      <div id="portfolio-items"></div>
    </div>

    <div id="servicos" class="tab-panel">
      <ul id="services-list"></ul>
    </div>

    <div id="avaliacoes" class="tab-panel">
      <div id="reviews-list"></div>
    </div>
  </section>

</main>

<script>
// Link para voltar ao perfil do usuário logado e mostrar avatar
document.addEventListener('DOMContentLoaded', function() {
  const userId = localStorage.getItem('userId') || sessionStorage.getItem('userId');
  const voltarPerfil = document.getElementById('voltar-perfil');
  const headerAvatar = document.getElementById('header-avatar');
  if (userId && voltarPerfil) {
    voltarPerfil.href = 'perfilcliente.html?id=' + encodeURIComponent(userId);
    // Buscar imagem de perfil do usuário logado
    fetch('api/get_cliente_by_id.php?id=' + encodeURIComponent(userId))
      .then(r => r.json())
      .then(cliente => {
        if (cliente && cliente.imagem) {
          headerAvatar.src = 'assets/images/' + cliente.imagem;
        } else {
          headerAvatar.src = 'assets/images/default.png';
        }
      });
  }
});
/* =========================
   VERIFICAÇÃO DE SESSÃO
========================= */
const urlParams = new URLSearchParams(window.location.search);
let userId = urlParams.get('id') || localStorage.getItem('userId') || sessionStorage.getItem('userId');

// Se não há id na URL, mas existe no localStorage/sessionStorage, redireciona para perfil.html?id=ID
if (!urlParams.get('id') && userId) {
  window.location.href = 'perfil.html?id=' + encodeURIComponent(userId);
}

// Se não há id em nenhum lugar, mostra erro ou redireciona para login
if (!userId) {
  document.getElementById('nome').textContent = 'Erro: sessão não encontrada.';
  // window.location.href = 'login.html'; // Descomente se quiser redirecionar
} else {
  // NÃO sobrescrever o userId do login!
  console.log('ID usado:', userId);
  fetch('api/get_user_type.php?id=' + encodeURIComponent(userId))
    .then(r => r.json())
    .then(resp => {
      console.log('Resposta get_user_type:', resp);
      if (resp.erro) {
        document.getElementById('nome').textContent = 'Erro: ' + resp.erro;
        return;
      }
      // 👉 CLIENTE: redireciona para perfilcliente
      if (resp.tipo === 'cliente') {
        window.location.href = 'perfilcliente.html?id=' + userId;
        return;
      }
      // 👉 FOTÓGRAFO CONTINUA
      carregarPerfil(urlParams.get('id') || userId);
    })
    .catch((err) => {
      document.getElementById('nome').textContent = 'Erro de sessão.';
    });
}


/* =========================
   CARREGAR PERFIL
========================= */
function carregarPerfil(userId) {
  const params = new URLSearchParams(window.location.search);
  const perfilId = params.get('id') || userId;

  fetch('api/fotografo_perfil.php?id=' + encodeURIComponent(perfilId))
    .then(r => r.json())
    .then(f => {

      if (f.erro) {
        document.getElementById('nome').textContent = f.erro;
        return;
      }

      document.getElementById('nome').textContent = f.nome;
      document.getElementById('descricao').textContent = f.descricao || '';
      document.getElementById('avaliacoes').textContent =
        f.avaliacao ? `⭐ ${f.avaliacao}` : '';
      document.getElementById('localizacao').textContent =
        f.distrito && f.concelho ? `${f.distrito}, ${f.concelho}` : '';

      if (f.imagemperfil) {
        document.getElementById('avatar').src = f.imagemperfil;
      }

      // serviços
      const services = document.getElementById('services-list');
      services.innerHTML = '';
      if (f.Categoria) {
        services.innerHTML = `<li> <strong>${f.Categoria}</strong></li>`;
      }
      if (f.servicos) {
        f.servicos.split(',').forEach(s => {
          services.innerHTML += `<li>${s.trim()}</li>`;
        });
      } else if (!f.Categoria) {
        services.innerHTML = '<li>Sem serviços.</li>';
      }

      // portfólio
      const portfolio = document.getElementById('portfolio-items');
      portfolio.innerHTML = '';
      if (Array.isArray(f.portfolio)) {
        f.portfolio.forEach(img => {
          portfolio.innerHTML += `<img src="${img}" style="max-width:180px;margin:8px;border-radius:12px;">`;
        });
      } else {
        portfolio.innerHTML = '<span style="color:#888">Sem portfólio.</span>';
      }
    });
}

/* =========================
   TABS
========================= */
document.querySelectorAll('.tab-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
    document.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'));
    btn.classList.add('active');
    document.getElementById(btn.dataset.tab).classList.add('active');
  });
});
</script>

{{/main}}
{{/layout}}
